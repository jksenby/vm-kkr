import { Routes } from '@angular/router';
import { VmPageComponent } from './component/vm-page/vm-page.component';

export const routes: Routes = [
  { path: '', component: VmPageComponent, title: 'Список вм' },
];
